package abstractFactoryDesignPattern.products.abstractProducts;

import abstractFactoryDesignPattern.UIComponentsConfig;

// abstract product
public interface Button {
    void render(UIComponentsConfig config);
}
