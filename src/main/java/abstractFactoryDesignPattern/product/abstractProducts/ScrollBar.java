package abstractFactoryDesignPattern.product.abstractProducts;

import abstractFactoryDesignPattern.UIComponentsConfig;

// abstract product
public interface ScrollBar {
    void scroll(UIComponentsConfig config);
}
